
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  scrollRestoration: true,

}
module.exports = nextConfig

module.exports = {
  eslint: {
    dirs: ['Exercises', 'test'],
  },
}